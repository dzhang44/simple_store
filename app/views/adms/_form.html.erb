<%= form_for(@adm) do |f| %>
  <% if @adm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adm.errors.count, "error") %> prohibited this adm from being saved:</h2>

      <ul>
      <% @adm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
