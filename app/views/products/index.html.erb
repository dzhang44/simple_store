<h1>Listing products</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Price</th>
    <th>Stock quantity</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.title %></td>
    <td><%= product.description %></td>
    <td><%= product.price %></td>
    <td><%= product.stock_quantity %></td>
    <td><%= link_to 'Show', product %></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <td><%= link_to 'Destroy', product, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>

<tr>
  <td><%=  "the average price of products is  " %><%= Product.average(:price).round(2) %> </td>
</tr>

<tr>
  <td><%=  "the maximum price is " %><%= Product.maximum(:price) %> </td>
</tr>

<tr>
  <td><%=  "the maximum price is " %><%= Product.maximum(:price) %> </td>
</tr>

<tr>
  <td><%=  "total number of unique products are " %><%= Product.count(:title) %> </td>
</tr>

<tr>
  <td><%=  "quantity of stock are " %><%= Product.sum(:stock_quantity) %> </td>
</tr>

<!--<% 100.times{ puts 'Chunky Bacon'} %>-->
<% 100.times do|i|%>
 <tr>   
   <!-- <td> Chunky Bacon </td>-->
    <td><%= i %> <%=  "Chunky Bacon" %></td>
    
  </tr>
<% end %>
 
</table>

<br />

<%= link_to 'New Product', new_product_path %>
